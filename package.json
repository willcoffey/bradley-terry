{
  "name": "bradley-terry",
  "description": "This is intended to be a simple implementation of the [Bradley-Terry][1] model. Code is written for easy of readability and verifiability over performance.",
  "bugs": {
    "url": "https://github.com/willcoffey/bradley-terry/issues"
  },
  "main": "bradley-terry.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev_readme": "nodemon --watch README.md --exec \"markdown_compile README.md > index.html\"",
    "build-gsheet": "esbuild gsheets-wrapper.ts --watch --bundle --format=esm --legal-comments=inline --tree-shaking=false --outfile=dist/gsheets-wrapper.js"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}
